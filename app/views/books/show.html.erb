<main>
  <p id="notice"></p>
  <div class='container px-5 px-sm-0'>
    <div class='row'>
      <div class='col-md-3'>
        <%= render "users/user_info", user: @user, book: @book %>
      </div>

      <div class='col-md-8 offset-md-1'>
        <h2>Book detail</h2>
        <table class='table table-hover table-inverse'>
          <tbody>
            <tr>
              <td>
                <%= link_to image_tag("/assets/images/no-image.jpg", class:"attachment user profile_image fallback"), user_path(@book.user_id) %>
              </td>
              <td><%= link_to @book.title, book_path(@book.id) %></a></td>
              <td><%= @book.body %></td>
              <td><%= link_to "edit", edit_book_path(@book.id), {class: "btn btn-sm btn-success"} %></td>
              <td><%= link_to "delete", destroy_book_path(@book.id), {method: :delete, data: {"confirm" => "本当に消しますか？"}, class: "btn btn-sm btn-danger"} %></td>
            </tr>
            <tr><td><%= link_to @user.name, user_path(@user.id) %></td></tr>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</main>